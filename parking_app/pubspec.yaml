name: parking_app
description: Ứng dụng quản lý bãi giữ xe thông minh.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.3  # ← ỔN ĐỊNH, KHÔNG LỖI PIGEON
  firebase_database: ^11.3.10  # ← TƯƠNG THÍCH VỚI firebase_core ^3.8.0
  provider: ^6.1.1
  cupertino_icons: ^1.0.2
  mobile_scanner: ^3.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true